{
  "name": "de-task-journal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run --reporter=default",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "happy-dom": "^20.0.10",
    "jsdom": "^27.0.1",
    "vitest": "^2.1.8"
  }
}
